{
  "name": "loadgo-arunachal",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspace @loadgo/api",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  }
}
